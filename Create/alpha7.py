#------------------------------------------------------------------------------

def run_formula(dv):
    alpha7 = dv.add_formula('alpha7', '-PB-PS', is_quarterly=False, add_data=True)
    return alpha7